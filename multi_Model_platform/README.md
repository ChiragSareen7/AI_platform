# Multi Model Platform

## Start
1. Create `.env` from `.env.example`
2. Install deps: `pip install -r requirements.txt`
3. Start local models API (`/models` project) on port `8010`
4. Run: `python run.py`

## Endpoints
- `POST /query`
- `GET /report`
